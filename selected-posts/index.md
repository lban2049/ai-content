# Selected Posts

经过筛选后保留下来的帖子，单独存放在这里。

这不是 `inbox`，也不是 `digests`。

用途：

- 给人工集中阅读
- 保留原帖链接、原始内容、中文翻译、AI 总结
- 作为阅读输入和观察池

边界：

- 这里只是阅读输入，不是可直接发布的素材池
- 如果后续要进入 `inbox`、topic 或文章，必须补充自己的实践验证

文件规则：

- 按日期存放：`YYYY-MM-DD.md`
- 追加式写法，不覆盖历史
- 每条帖子只记录一次
