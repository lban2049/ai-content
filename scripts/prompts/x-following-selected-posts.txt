请将通过第二轮过滤的帖子写入 `selected-posts/YYYY-MM-DD.md`。

要求：

- 只处理输入 JSON 中已有的通过项
- 不要调用 opencli，不要去抓 X
- 每条记录都按 `selected-posts/` 目录规则输出
- 原文是英文时，必须补中文翻译
- AI 总结保持简洁，聚焦“这条在说什么 / 为什么值得读 / 可关注的信息点”
- 如果帖子已存在，不要重复追加
